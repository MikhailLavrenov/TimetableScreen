<Window x:Class="TimetableScreen.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimetableScreen"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:vm="clr-namespace:TimetableScreen.ViewModels"
        mc:Ignorable="d"
        
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen"
        prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid d:DataContext="{d:DesignInstance  vm:ShellViewModel }">

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>


        <TextBox Grid.Row="0"
                 Grid.Column="1"
                 Text="{Binding ClientText}" />

        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Text="{Binding ServerText}" />

        <Button Grid.Row="2"
                Grid.Column="1"
                Content="Послать" Command="{Binding ClientSendCommand}"/>


    </Grid>
</Window>
